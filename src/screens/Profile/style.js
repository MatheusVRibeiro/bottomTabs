import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f1f8e9', // Cor de fundo clara
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});